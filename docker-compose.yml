services:
  recorddrive:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - recorddrive_data:/app/data
    restart: unless-stopped

volumes:
  recorddrive_data:
