<div
  hidden
  data-client-i18n
  data-show="<%= t('Show') %>"
  data-hide="<%= t('Hide') %>"
  data-show-password="<%= t('Show password') %>"
  data-hide-password="<%= t('Hide password') %>"
  data-confirm-continue="<%= t('Do you want to continue?') %>"
  data-file="<%= t('file') %>"
  data-files="<%= t('files') %>"
  data-item="<%= t('item') %>"
  data-items="<%= t('items') %>"
  data-uploading="<%= t('Uploading…') %>"
  data-no-item-selected="<%= t('No item selected') %>"
  data-one-selected="<%= t('1 selected · {{name}}', { name: '{{name}}' }) %>"
  data-delete-file-confirm="<%= t("Permanently delete '{{name}}'?", { name: '{{name}}' }) %>"
  data-all-files="<%= t('All files') %>"
  data-open-preview="<%= t('Select the Preview tab to open this file.') %>"
  data-preview-failed="<%= t('The file preview could not be loaded.') %>"
  data-spreadsheet-truncated="<%= t('Previewing the first {{rows}} rows and {{columns}} columns.', { rows: '{{rows}}', columns: '{{columns}}' }) %>"
  data-encrypted-archive="<%= t('This ZIP archive is password-protected.') %>"
  data-encrypted-archive-detail="<%= t('Archive contents are not shown for encrypted ZIP files.') %>"
  data-empty-archive="<%= t('This ZIP archive is empty.') %>"
  data-archive-contents="<%= t('Archive contents') %>"
  data-archive-truncated="<%= t('Only the first {{count}} archive entries are shown.', { count: '{{count}}' }) %>"
  data-pdf-preview-title="<%= t('PDF preview for {{name}}', { name: '{{name}}' }) %>"
  data-open-pdf-new-tab="<%= t('Open PDF in a new tab') %>"
  data-preview-unavailable="<%= t('Preview is not available for this file type.') %>"
  data-preview-requires-download="<%= t('Preview requires download permission.') %>"
  data-preview-requires-download-detail="<%= t('Ask the repository owner for download access.') %>"
  data-loading-preview="<%= t('Loading preview…') %>"
></div>
